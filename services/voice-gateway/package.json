{
  "name": "@voai/voice-gateway",
  "version": "0.0.0",
  "private": true,
  "description": "Voice Gateway — LiveKit room lifecycle, participant token issuance, AI bot orchestration for voice meetings",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b -w",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@types/express": "^4.17.21"
  },
  "dependencies": {
    "@voai/types": "workspace:*",
    "@voai/telemetry": "workspace:*",
    "@voai/errors": "workspace:*",
    "@voai/auth-context": "workspace:*",
    "express": "^4.21.0",
    "livekit-server-sdk": "^2.6.0"
  }
}
