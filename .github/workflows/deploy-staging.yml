name: Deploy to staging

# Sprint 1.1.3 — Staging deployment pipeline.
# This workflow is a placeholder. Cloud provider (AWS or GCP) and IaC
# (Terraform) wiring lands in Sprint 1.1.3 once the cloud decision is locked
# (Architecture v2 §10).

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    name: Build and deploy
    runs-on: ubuntu-latest
    if: false # disabled until Sprint 1.1.3
    steps:
      - uses: actions/checkout@v4
      - run: echo "Deploy implementation pending Sprint 1.1.3"
