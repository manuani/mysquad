{
  "name": "@voai/media-coordinator",
  "version": "0.0.0",
  "private": true,
  "description": "Media Coordinator — WebRTC/LiveKit session coordination, Deepgram STT, ElevenLabs TTS",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b -w",
    "start": "node dist/index.js",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0"
  },
  "dependencies": {
    "@voai/types": "workspace:*",
    "@voai/telemetry": "workspace:*",
    "@voai/auth-context": "workspace:*",
    "express": "^4.21.0",
    "livekit-server-sdk": "^2.6.0",
    "@deepgram/sdk": "^3.9.0",
    "elevenlabs": "^0.17.0"
  }
}
